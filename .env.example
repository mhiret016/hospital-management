# Database Configuration
DB_URL=jdbc:mysql://localhost:3308/eva_hospital_db
DB_USERNAME=root
DB_PASSWORD=root

# JWT Configuration
# Generate a secure random secret: https://generate-random.org/api-key-generator
# For development, you can use the example below, but CHANGE IT for any shared environment
JWT_SECRET=YourSecureRandomJWTSecretKeyHere_ChangeThisInProduction_MinimumLength32Characters
JWT_EXPIRATION=3600000

# Application Configuration
SPRING_PROFILES_ACTIVE=dev

# Notes for Developers:
# 1. Copy this file to .env (not tracked by git)
# 2. Update the values with your local configuration
# 3. NEVER commit the .env file to git
# 4. Generate your own JWT_SECRET using a secure random generator
